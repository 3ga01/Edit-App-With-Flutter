import 'package:flutter/material.dart';

class SignOn extends StatelessWidget {
  const SignOn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Column(
            children: [Text("GROCERY"), Text("MART")],
          )
        ],
      ),
    );
  }
}
